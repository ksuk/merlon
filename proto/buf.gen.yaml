version: v2
plugins:
  - remote: buf.build/protocolbuffers/go
    out: ../api/gen
    opt: paths=source_relative
  - remote: buf.build/grpc/go
    out: ../api/gen
    opt: paths=source_relative
  # Rust generation via tonic-build in engine/build.rs
  # No buf plugin needed for Rust
