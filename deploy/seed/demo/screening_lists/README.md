# Synthetic screening lists

The public demo must never load production or real-world sanctions/PEP data.
If screening is enabled in a local fork, only synthetic records with the
`DEMO-` prefix may be mounted here. The Northflank demo keeps screening import
and rescreening disabled and does not expose this directory.
